name: CI

on:
  push:
    branches: [ AaronNgrx ]
  pull_request:
    branches: [ AaronNgrx ]
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-lastest
    steps:
      - uses: actions/checkout@v2
      - name: Run a one-line script
        run: echo CI test~
